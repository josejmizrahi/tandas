// AUTOGENERATED. Do not edit. Source: ios/Tandas/Platform/Models/ResourceType.swift
// Run: make gen

import Foundation

public extension ResourceType {
    public static let knownCases: [ResourceType] = [
        .event,
        .slot,
        .fund,
        .position,
        .asset,
        .contribution,
    ]

    public static let knownRawValues: Set<String> = [
        "event",
        "slot",
        "fund",
        "position",
        "asset",
        "contribution",
    ]

    public init(from decoder: Decoder) throws {
        let raw = try decoder.singleValueContainer().decode(String.self)
        self = Self.from(raw: raw)
    }

    public func encode(to encoder: Encoder) throws {
        var c = encoder.singleValueContainer()
        try c.encode(self.rawString)
    }

    public var rawString: String {
        switch self {
        case .event: return "event"
        case .slot: return "slot"
        case .fund: return "fund"
        case .position: return "position"
        case .asset: return "asset"
        case .contribution: return "contribution"
        case .unknown(let s): return s
        }
    }

    public static func from(raw: String) -> ResourceType {
        switch raw {
        case "event": return .event
        case "slot": return .slot
        case "fund": return .fund
        case "position": return .position
        case "asset": return .asset
        case "contribution": return .contribution
        default: return .unknown(raw)
        }
    }
}
