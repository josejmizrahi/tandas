// AUTOGENERATED. Do not edit. Source: ios/Tandas/Platform/Models/PermissionLevel.swift
// Run: make gen

import Foundation

public extension PermissionLevel {
    public static let knownCases: [PermissionLevel] = [
        .founder,
        .anyMember,
        .majorityVote,
        .supermajorityVote,
        .host,
        .treasurer,
    ]

    public static let knownRawValues: Set<String> = [
        "founder",
        "anyMember",
        "majorityVote",
        "supermajorityVote",
        "host",
        "treasurer",
    ]

    public init(from decoder: Decoder) throws {
        let raw = try decoder.singleValueContainer().decode(String.self)
        self = Self.from(raw: raw)
    }

    public func encode(to encoder: Encoder) throws {
        var c = encoder.singleValueContainer()
        try c.encode(self.rawString)
    }

    public var rawString: String {
        switch self {
        case .founder: return "founder"
        case .anyMember: return "anyMember"
        case .majorityVote: return "majorityVote"
        case .supermajorityVote: return "supermajorityVote"
        case .host: return "host"
        case .treasurer: return "treasurer"
        case .unknown(let s): return s
        }
    }

    public static func from(raw: String) -> PermissionLevel {
        switch raw {
        case "founder": return .founder
        case "anyMember": return .anyMember
        case "majorityVote": return .majorityVote
        case "supermajorityVote": return .supermajorityVote
        case "host": return .host
        case "treasurer": return .treasurer
        default: return .unknown(raw)
        }
    }
}
